## 웹 검색 규칙

웹 검색이나 웹 사이트 접근이 필요할 때는 Chrome DevTools MCP를 필수로 사용한다
절대 하면 안되는 행동: playwright 설치 및 사용

## .env 관리 룰

모든 정보는 .env 에 저장한다
기본값은 .env 외의 코드에 넣지 않는다
(중요) .env 파일 외에 모든 곳에서는 기본값이 없으면 오류가 발생하는 것이 맞다